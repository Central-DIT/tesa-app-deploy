# tesa-docker-build-and-push
Reusable GitHub Actions workflows for building and pushing Docker images to Azure Container Registry (ACR). Used by multiple repositories for backend and service deployments.
